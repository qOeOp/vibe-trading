/* Stub: VT lab migration — dependency graph panel (not used in VT) */
'use client';

const DependencyGraphPanel = () => {
  return <div>Dependency graph not available</div>;
};

export default DependencyGraphPanel;
