/* Stub: VT lab migration — terminal component (not used in VT) */
'use client';

const Terminal = (_props: { visible?: boolean; onClose?: () => void }) => {
  return <div>Terminal not available</div>;
};

export default Terminal;
