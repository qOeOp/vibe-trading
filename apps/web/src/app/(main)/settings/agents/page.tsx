import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Agents - Settings - Vibe Trading",
};

export default function SettingsAgentsPage() {
  return (
    <div className="placeholder-center">
      Settings / Agents — Coming Soon
    </div>
  );
}
