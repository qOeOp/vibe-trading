import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Monitor - Risk - Vibe Trading",
};

export default function RiskMonitorPage() {
  return (
    <div className="placeholder-center">
      Risk / Monitor — Coming Soon
    </div>
  );
}
