import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Rules - Risk - Vibe Trading",
};

export default function RiskRulesPage() {
  return (
    <div className="placeholder-center">
      Risk / Rules — Coming Soon
    </div>
  );
}
